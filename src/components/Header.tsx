import { ReactElement } from "react"

const Header = (): ReactElement => {
    return (
    <header className="c-page-header-full-width ds-l-lg-col">
        <h1 className="ds-text-heading--5xl">Notetaker</h1>
    </header>
    )
}

export default Header
